GlobPlugins('scripting/*.sp')

Package(name='pugsetup',
        plugins=Plugins('pugsetup*'),
        disabled_plugins=Plugins('pugsetup_*'),
)

Package(name='pugsetup_base',
        plugins=['pugsetup'],
)
